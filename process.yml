apps:
  - script        : src/app.js
    instances     : max
    exec_mode     : cluster
    name          : buckless-server
    kill_timeout  : 3000
    listen_timeout: 3000
    error_file    : log/console.error.log
    out_file      : log/console.log
    wait_ready    : true
