module.exports = () => {
  console.log('→ ensure dev.inst.buckless.com is in the hostfiles');
  console.log('→ ensure dockercompose is up and running');
  console.log('→ run dev command in all the environments. to ditch will yarn v2');
};
